            <div class="brand">
                {% set homeUrl = "/" %}
                {% if lang and lang !== config.defaultLanguage %}
                {% set homeUrl = "/" + config.languages[lang].slug %}
                {% endif %}
                <a rel="home" href="{{ homeUrl }}"{% if page.url === homeUrl %} aria-current="page"{% endif %}>Trivet</a>
            </div>
